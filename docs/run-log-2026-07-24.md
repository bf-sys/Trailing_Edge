# Run Log — 2026-07-24

Live run of Agents 1 + 2 against two categories (ship/hazards priority set),
followed by a widened pass on puzzle-props after the first pass returned
nothing there, then a third pass narrowed to Phase 1 scope specifically
(GDD §12), then a partial Agent 3 (Prep) run.

Tools used: Kenney.nl site search, OpenGameArt.org advanced search, via live
browser (Chrome extension), not a generic web search API — searches were
manual per-category, matching Agent 1's spec.

## Owner decisions made mid-run (apply these on any re-run)
- Ship damage-state implementation: **overlay VFX**, not sprite-swap.
- Scope divisor for Agent 3's first pass: **GDD Phase 1 only** (§12), not the
  full Core asset list — narrows the puzzle-prop and hazard-variety problems
  down to exactly one hazard (Debris Field), one resupply point (Star), one
  puzzle element (Relay Beacon), rather than all taxonomy rows at once.
- CC-BY-SA accepted for this project (no redistribution planned) — see
  `ATTRIBUTION.md` for the standing note to re-check if that changes.

## Pass 1 — Ship, hazards, UI, VFX

| Query | Source | Hit |
|---|---|---|
| `space shooter` | kenney.nl | Space Shooter Remastered, Simple Space, UI Pack - Sci-Fi, Planets, Space Kit (3D, excluded) |
| `particle` | kenney.nl | Particle Pack (generic VFX textures), Smoke Particles |
| `space debris asteroid` | opengameart.org | Asteroids/Debris Set (exact fit); Space Game Art Pack Extended (Tatermand) |

### Coverage matrix

| Asset-list line item | Candidate | License | Format | Coverage | Note |
|---|---|---|---|---|---|
| Player ship base sprite | Kenney *Space Shooter Remastered* | CC0 | 295 individual PNGs | Partial | Generic ship silhouettes; none read as "older/reliable/proven" specifically — usable as placeholder |
| Ship damage-state VFX | Same pack (explosion/impact frames) | CC0 | individual PNGs | Partial | Supports overlay-VFX route (per art-list's own open flag), not sprite-swap route |
| Debris Field hazard | OpenGameArt *Asteroids/Debris Set* | CC0 | 9 sprites, 3 sizes, individual PNGs | **Full** | |
| Resource Field (resupply asteroids) | Same pack — separate debris sub-set | CC0 | same | **Full** | Pack already separates "asteroid" vs "debris" as distinct sprite sets — directly resolves the art-list's own flagged risk about these two needing to read as opposite in intent |
| Meteoroid | Kenney *Simple Space* / *Space Shooter Remastered* | CC0 | individual PNGs | Partial | Present but not yet confirmed distinct from a Comet candidate (see gap) |
| Ion Storm / Nebula Field | Kenney *Particle Pack* (raw smoke/glow) vs. OpenGameArt *Space Game Art Pack Extended* (pre-made "2 nebulas") | CC0 vs. **CC-BY-SA 3.0** | 512×512 textures vs. one 26MB layered PSD | Partial, both | Real license-tier choice: CC0/composite-yourself vs. CC-BY-SA/pre-made-but-extraction-heavy. Owner has approved CC-BY-SA for this non-distributed build. |
| HUD bars, ability icon frames | Kenney *UI Pack - Sci-Fi* | CC0 | 130 individual PNGs | **Full** | |
| Ability activation VFX | Kenney *Particle Pack* | CC0 | 512×512 textures | Partial | Raw compositing material, not pre-made ability effects |

## Pass 2 — Puzzle-site props (widened per owner approval)

| Query | Source | Result |
|---|---|---|
| `sci-fi crate prop 2d` | opengameart.org | 1 irrelevant hit (turret pack) |
| `space station props pack` | opengameart.org | zero results |
| `satellite beacon buoy` (compound) | opengameart.org | zero results — compound queries over ~3 terms return nothing even when individually-relevant packs exist |
| `satellite sprite` | opengameart.org | Buransk Asset Pack CC0 (3D .blend files, off-topic Earth-map assets, one usable `ring16.png` plain ring sprite, CC0); Space Sprites (2D sheet, tag includes "probes", **CC-BY 3.0**, bundles Lostgarden-sourced material with its own separate attribution requirement per a correction thread in the item's comments) |
| `beacon light sprite` | opengameart.org | zero results |

### Finding (not a gap to keep re-searching)
Cargo Pod, Wreckage, Relay Beacon, and Beacon Cluster have **no named match**
under either their in-fiction names or a first round of generic-shape terms.
Confirmed twice independently (crate/prop search and beacon/light search both
empty). Next step per Agent 1's spec: broaden to single generic nouns
("satellite dish sprite," "space station module 2d," "orb light sprite")
rather than continuing to search compound or in-fiction terms.

### Usable finds from this pass
- `ring16.png` (Buransk pack, CC0) — plausible reskin base for a beacon/marker,
  not a designed match
- *Space Sprites* pack (CC-BY 3.0) — has a probe-tagged sprite, useful for the
  level-objective "inactive probe" object, not just puzzle props; attribution
  needs to cover both the uploader and the originally bundled Lostgarden
  material

## Pass 3 — Phase 1 scope narrowing: Star + Relay Beacon

Divisor changed from "full asset list" to "GDD §12 Phase 1 only" per owner.
This drops Comet/Meteoroid and Ion Storm/Nebula Field from the blocking list
entirely (both Phase 2a/2b) and leaves exactly two open Phase-1 items: a
Star resupply sprite and a Relay Beacon puzzle element.

| Query | Source | Result |
|---|---|---|
| `planets` | kenney.nl | Planets pack (50 files, CC0) — planet spheres, no sprite clearly reads as a distinct sun/star body on inspection |
| `sun` | kenney.nl | zero results |
| (re-examined) | kenney.nl *Simple Space* | small star/sparkle icon sprites present alongside its ships/asteroids/stations |
| `beacon` | opengameart.org | **Beacons** (sound effects only, not visual — false lead from the name alone); **Industrial objects pack v1.0** (3D models, off-scope for a 2D Phaser project); **Warped Top-Down Tech Lab Extension** — explicit "Beacon (animated)" tile |

### Result

| Line item | Candidate | License | Coverage | Note |
|---|---|---|---|---|
| Star (resupply point) | Kenney *Simple Space* — star/sparkle icon sprite | CC0 | Partial | Style clash risk: *Simple Space* is flat minimalist line art, *Space Shooter Remastered* (ship source) is shaded/rendered. Flagged, not resolved — see manifest doc. |
| Relay Beacon (`SequenceSpotElement`) | OpenGameArt *Warped Top-Down Tech Lab Extension* — "Beacon (animated)" tile | CC0 | **Full** | Purpose-built, no reskinning needed |
| (incidental) Cargo Pod / Wreckage | Same sheet — "Crates & barrel" tile | CC0 | **Full** | Phase 2a item, found for free from the same source; not prepped in this pass |
| (incidental) Base tileset lead | OpenGameArt *Warped Top-Down Tech Lab* (Ansimuz) | CC0 | Unevaluated | Flagged as a lead for puzzle-site background tiles, not assessed in depth |

Net: every Phase 1 line item now has at least a Partial match; two of five
(Debris Field, Relay Beacon) are Full.

## Agent 3 (Prep) run — partial, environment-limited

Attempted a full Prep run against the Phase-1-cleared set. **Discovered a
real tooling constraint, not a process gap:** this environment's sandboxed
container cannot reach kenney.nl or opengameart.org directly (outside the
network allowlist), and files downloaded via the browser tool land in the
operator's local Chrome Downloads folder — a location the container also
cannot read. So Agent 3 could not execute the binary-handling half of its
own spec (format conversion, cropping, renaming, repackaging) end-to-end in
this run.

**What was completed:**
- Triggered direct-download actions in-browser for all five Phase-1 source
  files (owner confirmed, all CC0, no login required):
  Space Shooter Remastered, Simple Space, UI Pack - Sci-Fi, Asteroids/Debris
  Set, Warped Top-Down Tech Lab Extension.
- Produced `ATTRIBUTION.md` (full credit ledger, including deferred
  CC-BY/CC-BY-SA items not yet pulled in).
- Produced `phase1-manifest-and-tasks.md` (directory convention mapped to
  GDD §11.7, per-file extraction task list, placeholder-naming rule).

**What still requires manual action (or a different environment):**
- Moving the five downloaded files from Chrome's Downloads folder into the
  actual project directory.
- The extraction/cropping/renaming work itself (sprite-sheet splitting,
  tile-grid cropping on the Warped Tech Lab PNG) — listed step-by-step in
  `phase1-manifest-and-tasks.md` but not executable here.
- Resolving the Star/ship style-mismatch flag once both sprites can actually
  be viewed side by side in the target project.

## Carried-forward gaps (updated)
1. **Phase 1 asset prep is source-complete but not file-complete** — see
   Agent 3 section above; needs manual extraction to actually finish.
2. Star vs. ship style mismatch — unresolved judgment call, Phase 1 only.
3. Puzzle-prop generic-shape search beyond Relay Beacon/Cargo Pod (Beacon
   Cluster still unmatched) — Phase 2a/2b, not currently blocking.
4. Comet vs. Meteoroid — no confirmed distinct pair yet — Phase 2a/2b.
5. Ion Storm/Nebula Field — license-tier decision made (CC-BY-SA approved),
   style decision (composite-yourself vs. pre-made) still open — Phase 2a/2b.
