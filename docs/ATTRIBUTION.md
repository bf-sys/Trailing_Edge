# Attribution — Trailing Edge (Phase 1 asset set)

Generated by Agent 3 (Prep) per the Evaluation Agent's cleared line items,
Phase-1 scope only. Update this file as later phases add assets — don't
regenerate from scratch, append.

**2026-07-29 note:** two rows below describe assets whose *design role* was
reassigned by a GDD revision on this date (source/pack/license facts are
unchanged — only what the asset represents in-game). See `STATUS.md`'s
"Design update" section for the full explanation.

**2026-07-31 note:** the Home Marker row below was reassigned again —
the single Home Marker object split into Entry Wormhole and Exit Wormhole
(two distinct locations); the same sprite file now backs both instances,
tinted differently at runtime. Source/pack/license facts are unchanged.
See `STATUS.md`'s 2026-07-31 "Design update" section.

**2026-08-01 note:** five rows below were replaced entirely (not just
reassigned) by new AI-generated final art — ship, wormhole pair, AsteroidField
large resupply, Relay Beacon, plus the starfield backgrounds. Moved out of
the CC0-sourced table below into "Owner-created assets" — these are no
longer Kenney/OpenGameArt sprites at all, so their license facts changed,
unlike the earlier reassignment-only notes above. See `STATUS.md`'s
2026-08-01 "Design update" section.

## Assets requiring no attribution (CC0 — credit optional/voluntary)

| Asset | Source | Pack | License |
|---|---|---|---|
| Ship damage overlay / explosion frames (`ship/ship_damage_overlay_PLACEHOLDER.png`) | Kenney | Space Shooter Remastered | CC0 1.0 |
| HUD bars, panel/slider frames (unused in code as of 2026-08-10 — see `STATUS.md`; files moved to gitignored `art-staging/ui-unused/`, reserved not deleted) | Kenney | UI Pack - Sci-Fi 2.0 | CC0 1.0 |
| Debris Field hazard sprites, AsteroidField medium/small resupply sprites (large variant replaced 2026-08-01, see below) | OpenGameArt (author: The_Scientist___) | Asteroids/Debris Set | CC0 |
| Cargo Pod/Wreckage tile (Phase 2a, not yet extracted) | OpenGameArt (author: knekko, extending ansimuz) | Warped Top-Down Tech Lab Extension | CC0 (public domain release) |

No attribution is legally required for the above. Kenney and the two OGA
authors each note credit as voluntary/optional. **Recommended practice
regardless:** list them anyway in a game-credits screen or README, since it
costs nothing and is normal courtesy in this community — but it is not a
compliance requirement for this project.

**Not yet in this ledger:** Signal Array (unsourced as of 2026-07-29, see
above) — add a row here once sourced.

## Owner-created assets (no third-party source — not a licensing matter)

| Asset | Note |
|---|---|
| Player ship sprite (`ship/ship_base.png`) | AI-generated (2026-08-01) via the Art Director Agent / Google Gemini (`gemini-3.1-flash-image`), replacing the Kenney Space Shooter Remastered placeholder. No third-party source, no attribution obligation. |
| Probe sprite (`objectives/probe.png`) | AI-generated (2026-08-01) via the Art Director Agent / Gemini, replacing the owner's original greyscale placeholder (`probe_PLACEHOLDER.png`, added 2026-07-29). No third-party source. |
| Entry Wormhole / Exit Wormhole sprite (`objectives/wormhole.png`) | AI-generated (2026-08-01) via the Art Director Agent / Gemini, replacing the Kenney Simple Space placeholder. One file, reused for both instances, tinted differently at runtime (`waypointTintConfig`). No third-party source. |
| AsteroidField large resupply sprite (`resupply/asteroid_large.png`) | AI-generated (2026-08-01) via the Art Director Agent / Gemini, replacing the OpenGameArt Asteroids/Debris Set placeholder. Medium/small variants are unaffected — still the OGA-sourced placeholder, see the CC0 table above. No third-party source. |
| Relay Beacon sprite (`objectives/relay_beacon.png`) | AI-generated (2026-08-01) via the Art Director Agent / Gemini, replacing the Kenney/OpenGameArt satellite sprite + generated glow overlay pair. Now a single asset, tinted per `waypointTintConfig` instead of a separate reached-overlay file (`RelayBeaconObject`). No third-party source. |
| Starfield background tiles (`backgrounds/bg_stars_far.jpg`, `backgrounds/bg_stars_near.jpg`) | AI-generated (2026-08-01) via the Art Director Agent / Gemini, replacing the procedurally generated placeholder tiles (`StarfieldBackground.ts`). No third-party source. |
| Meteoroid hazard sprite (`hazards/hazard_meteoroid.png`) | **Content replaced 2026-08-22.** Original (AI-generated 2026-08-19/20 via the art Generate-Evaluate-Refine loop, passed round 1 clean) had its ember/flame trail exiting at a visibly shallower angle than the rock silhouette's own long axis -- flagged on direct review. Regenerated as candidate `meteoroid_v2` in `tools/art-reviewer/` (base prompt unchanged, iterated with direct feedback rather than the full automated GER loop -- two attempts anchored too hard on the flawed original via `useReference`, the winning attempt dropped the reference and described the geometry in text instead) so the trail now runs collinear with the rock's own axis. **Integrated on the project owner's direct visual approval**, same precedent as the Nebula Field variant-2 row below. Chroma-keyed and auto-cropped by `tools/asset-prep/chroma-key.js`. `hazardConfig.ts`'s `meteoroid.spriteFacingOffsetRadians` was recalibrated alongside it (measured from the new pixel data, `tools/asset-prep/measure-facing-angle.js`) since it's derived from this exact sprite's trail angle -- old value (+30 deg) would have been wrong for the new art. The original `meteoroid.jpg`/`meteoroid_v2.jpg` candidates both remain in `tools/art-reviewer/assets/` for reference. No third-party source. See `docs/history/art-eval-log-2026-08-19.md` for the original round; this replacement wasn't run through a fresh Evaluate pass. |
| Ion Storm hazard sprite (`hazards/hazard_ion_storm.png`) | AI-generated (2026-08-19/20) via the same art GER loop / Gemini, replacing the flat generated placeholder circle. Passed evaluation round 1 clean. Generated as an independently-designed sprite rather than the shared Ion Storm/Nebula Field texture-family approach `art-production-guidelines.md` documents — a deliberate one-off deviation, see `STATUS.md`'s 2026-08-19 entry. No third-party source. |
| Nebula Field hazard sprite (`hazards/hazard_nebula_field.png`) | **Content replaced 2026-08-21** — was candidate `nebula_field` (AI-generated 2026-08-19/20, oblong ~1.9:1 result), now candidate `nebula_field_1` ("Core Bloom"), part of the 2026-08-21 multi-variant pass `art-production-guidelines.md` originally called for. Passed evaluation round 1 clean (Technique 8, Style 8, Format 9), specifically re-biased toward a circular silhouette (measured 622x616px, 1.010:1). The original `nebula_field` candidate's pixel content no longer exists under any file in `assets/hazards/` — dropped from the game's rotation entirely per the project owner's direct review (see STATUS.md's 2026-08-21 follow-up entry), not merely deprioritized. Chroma-keyed and auto-cropped by `tools/asset-prep/chroma-key.js`. No third-party source. See `docs/history/art-eval-log-2026-08-21.md`. |
| Nebula Field hazard sprite variant 2 (`hazards/hazard_nebula_field_alt2.png`) | AI-generated (2026-08-21) via the art GER loop / Gemini, candidate id `nebula_field_2` ("Layered Rings"). Passed round 1 clean (8/8/8), but the project owner judged that accepted image "exceedingly regular" on direct review; a rougher-edges redo achieved that (edges confirmed organic against the other two variants) but the automated evaluator's bbox scan read the silhouette as regressing to oblong (~1.786:1) across two refine rounds, hitting the 3-round GER cap at an `escalate` verdict. **Integrated anyway, 2026-08-21, on the project owner's direct visual approval of the round-3 image — a human override of the automated escalation, not a retroactive automated pass.** The actual chroma-keyed/auto-cropped output measured 694x716px (0.969:1) — reasonably circular by both direct visual inspection and this alternate (alpha-threshold, not RGB-patch-distance) measurement method. Round-3's feedback folded into `assets.json`'s base prompt. Chroma-keyed and auto-cropped by `tools/asset-prep/chroma-key.js`. No third-party source. See `docs/history/art-eval-log-2026-08-21.md`'s `nebula_field_2` rounds 1-3. |
| Nebula Field hazard sprite variant 3 (`hazards/hazard_nebula_field_alt3.png`) | AI-generated (2026-08-21) via the art GER loop / Gemini, candidate id `nebula_field_3` ("Drifting Wisp"). Round 1 flagged on Format (an unrequested two-cloud contact-sheet artifact); round 2 passed after regeneration feedback (Technique 8, Style 8, Format 9), measured 526x502px (1.048:1) — circular. Chroma-keyed and auto-cropped by `tools/asset-prep/chroma-key.js`. No third-party source. See `docs/history/art-eval-log-2026-08-21.md`. |

## Assets NOT included in this Phase-1 pull (deferred, license flagged for later)

These were evaluated in earlier passes but are **out of Phase-1 scope** and
were not downloaded in this run. Listed here so the attribution obligation
isn't lost when they're pulled in during Phase 2a/2b:

| Asset | Source | License | Obligation when used |
|---|---|---|---|
| Space Sprites (probe-tagged sheet) | OpenGameArt (author: titmouse001) | **CC-BY 3.0** | Requires attribution to titmouse001. Also note: the pack's own comment thread records that it originally bundled Lostgarden-sourced material under a mismatched license, corrected after a licensing review — if any Lostgarden-originated sprite is used, attribute "Sinistar clone / Hard Vacuum / Remastered Tyrian graphics by Daniel Cook (Lostgarden.com)" separately, per the item's own copyright notice. |
| Space Game Art Pack (Extended) — nebula layers | OpenGameArt (author: Tatermand) | **CC-BY-SA 3.0** | Share-alike: any modified/derivative asset extracted from this PSD, if distributed, must be released under CC-BY-SA 3.0 or compatible. Project owner has confirmed this build is not being distributed, so the share-alike clause does not currently trigger — re-check this note if distribution scope changes. |

## Policy note
Per project owner: this build is a class project and will not be
distributed, so CC-BY-SA's share-alike obligation is currently inactive.
This file still records per-item license terms so that determination can be
correctly revisited if the project's distribution status ever changes.
